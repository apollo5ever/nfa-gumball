export function findPurchased(old, current) {


      return old.filter(value => !current.includes(value));

    
    
  
    // Iterate through the first array and increment counts in the map
    for (const element of arr1) {
     if(!arr2.includes(element)){
        return element
     }
    }

    for (const element of arr2) {
        if(!arr1.includes(element)){
           return element
        }
       }
  
 
  }
  

  